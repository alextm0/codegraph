% =============================================================================
% Chapter 4: Implementation
% =============================================================================

\chapter{Implementation}
\label{ch:implementation}

% =============================================================================
\section{System Architecture}
\label{sec:system-architecture}
% =============================================================================

Chapter~3 described the retrieval method. This chapter describes the implementation that makes the method usable from the command line, the MCP server, and the visualizer. The focus shifts from why each stage exists to how the codebase organizes parsing, graph writes, retrieval, and delivery.

The implementation separates stable core logic from user-facing interfaces. Parser and graph modules build the Neo4j representation; retrieval modules select seeds, run PPR, and format context; CLI, MCP, and visualizer modules call the same retrieval pipeline rather than reimplementing ranking logic. \Cref{tab:implementation-modules} summarizes these boundaries.

\begin{table}[H]
    \centering
    \scriptsize
    \caption[Implementation module boundaries]{Primary implementation modules and their responsibilities. The same core retrieval path is shared by CLI, MCP, and visualizer entry points.}
    \label{tab:implementation-modules}
    \renewcommand{\arraystretch}{1.25}
    \begin{adjustbox}{max width=\textwidth}
    \begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.4cm}
                                  >{\raggedright\arraybackslash}X
                                  >{\raggedright\arraybackslash}p{4.2cm}}
        \toprule
        \rowcolor{black!6}
        \textbf{Package} & \textbf{Responsibility} & \textbf{Representative files} \\
        \midrule
        \texttt{core/parser} & Tree-sitter parsing and entity extraction & \texttt{python\_parser.py}, \texttt{extractors.py}, \texttt{models.py} \\
        \texttt{core/graph} & Neo4j writes, static resolution, PPR projection, dependency queries & \texttt{graph\_builder.py}, \texttt{resolution.py}, \texttt{ppr.py} \\
        \texttt{core/retrieval} & Seed selection, IDF reweighting, PPR orchestration, context formatting & \texttt{pipeline.py}, \texttt{seed\_selection.py}, \texttt{post\_processing.py} \\
        \texttt{mcp} & FastMCP server lifecycle and tool implementations & \texttt{server.py}, \texttt{tools.py}, \texttt{server\_config.py} \\
        \texttt{cli} and \texttt{visualizer} & User interfaces over the same core pipeline & \texttt{commands/query.py}; visualizer routes \\
        \bottomrule
    \end{tabularx}
    \end{adjustbox}
\end{table}

The engineering policy is conservative: prefer deterministic extraction, skip ambiguous edges, and route every interface through the same retrieval core. The following sections describe the main components in that order.

% =============================================================================
\section{The Parser: Extracting Structure from Code}
\label{sec:the-parser}
% =============================================================================

The foundational stage of CodeGraph is the ingestion pipeline, where we transform raw software repositories into structured representations of code entities.

We use Tree-sitter, an incremental parser called through its Python bindings, for extraction. Tree-sitter produces a Concrete Syntax Tree (CST) that maps directly to source byte offsets. This byte-level mapping lets the parser preserve formatting and tolerate syntax errors, which matters in development environments where code is often in an intermediate state.

Rather than indexing every variable assignment or loop, we implement a custom extraction algorithm that traverses the CST to identify four architectural entities: \texttt{File}, \texttt{Class}, \texttt{Function}, and \texttt{Method}. \noindent For each entity, we store the following metadata:
\begin{itemize}
    \item \textbf{Qualified Name (QN):} A globally unique identifier used as the primary node key, such as \texttt{auth.py::}\texttt{AuthService.login}.
    \item \textbf{Line Boundaries:} The start and end line numbers used to extract code snippets for the context package.
    \item \textbf{Signatures and Docstrings:} The textual corpus used for BM25 keyword matching during seed selection.
\end{itemize}

\begin{figure}[htbp]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \begin{cgpython}{Source file}
def login(user, pwd):
    """Authenticate a user."""
    entry = db.get_user(user)
    if entry.check_pwd(pwd):
        return Token(entry.id)
    return None
        \end{cgpython}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.48\textwidth}
        \begin{cgjson}{Extracted entity}
{
  "type": "Function",
  "name": "login",
  "qualified_name": "auth.py::login",
  "start_line": 1,
  "end_line": 6,
  "signature": "(user, pwd)",
  "docstring": "Authenticate a user."
}
        \end{cgjson}
    \end{minipage}
    \caption[Parsing output example]{Side-by-side comparison of a raw Python function and the corresponding structural entity extracted by the parser.}
    \label{fig:parsing-example}
\end{figure}

\Cref{fig:parsing-example} illustrates this extraction process. The parser ignores local implementation details and stores only the metadata needed by later stages: identity, location, signatures, and docstrings.

% =============================================================================
\section{The Resolver: Connecting the Pieces}
\label{sec:the-resolver}
% =============================================================================

We begin by extracting independent entities, but this is only the first step. To construct a graph capable of propagating relevance, we must determine how these entities interact. Our resolver converts raw string references into concrete graph edges.

\subsection{Import Resolution}

Import statements declare cross-file dependencies. Our implementation maps Python import strings into physical filesystem paths. For absolute imports, such as \texttt{import core.utils}, the resolver maps the dotted path relative to the project root. For relative imports, such as \texttt{from ..models import User}, it resolves parent directories before mapping. We identify external packages and standard library imports via dependency manifests and intentionally omit them to avoid creating dangling nodes that point outside the repository scope.

\subsection{Call Resolution: The Fallback Protocol}

Linking a function call expression, such as \texttt{verify()}, to the correct definition node in a dynamically typed language like Python requires a conservative strategy. \noindent We use a three-level resolution protocol, executed in descending order of confidence:

\begin{enumerate}
    \item \textbf{Explicit Imports:} We first check the file's import statements. If the called name matches a name imported from another module, we link the call to the corresponding entity in that module.
    \item \textbf{Local Scope:} If no import matches, we search for a function or method defined within the same file. This handles intra-file calls and class-level method invocations.
    \item \textbf{Global Scope:} As a final resort, we query the entire repository index for any entity with a matching name. This broad search captures calls where the import logic is too complex for static analysis.
\end{enumerate}

\subsection{Precision over Recall}

Across all resolution logic, we prefer precision over recall. If a global search finds multiple candidates due to ambiguous naming, such as multiple \texttt{update} methods, the resolver skips the connection. Because Personalized PageRank propagates along every edge, a false edge can send relevance mass into an unrelated subgraph. Dropping ambiguous connections loses some recall, but it keeps the graph more trustworthy.

Real-world edge cases support this decoupled approach. The system handles circular imports without recursive loops because it analyzes files independently. Runtime import calls and plugin loaders cannot be resolved statically without execution tracing, so the resolver skips them.

\section{The Database: Storing the Graph}
\label{sec:the-database}
% =============================================================================

After parsing the code and resolving connections, we persist the data into a structural graph. CodeGraph uses Neo4j, a specialized graph database optimized for relationship-centric storage. This architectural choice allows multi-hop traversals to execute in milliseconds, providing a performance advantage over the recursive joins required by traditional relational databases.

\subsection{Schema and Constraints}

We designed the graph schema to reflect our four-entity model (\texttt{File}, \texttt{Class}, \texttt{Function}, \texttt{Method}). We connect these nodes using four relationship types, as shown in \cref{fig:database-schema}. We use a visual hierarchy where containers (\texttt{File} and \texttt{Class}) are structurally prominent, while functional units (\texttt{Function} and \texttt{Method}) represent the leaf nodes of the hierarchy.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        node distance=1.6cm and 2.4cm,
        el/.style={font=\scriptsize\ttfamily, fill=white, inner sep=1pt},
    ]
        \node[cgnodefile, minimum width=2.2cm] (file) {File};
        \node[cgnodeclass, minimum width=2.2cm, right=of file] (class) {Class};
        \node[cgnodefunc, minimum width=2.2cm, below=1.1cm of file] (func) {Function};
        \node[cgnodefunc, minimum width=2.2cm, below=1.1cm of class] (method) {Method};

        \draw[lattice_gray_arrow] (file) -- node[el, above=2pt] {CONTAINS} (class);
        \draw[lattice_gray_arrow] (file) -- node[el, left=2pt] {CONTAINS} (func);
        \draw[lattice_gray_arrow] (class) -- node[el, right=2pt] {CONTAINS} (method);
        \draw[lattice_arrow] (func) -- node[el, above=2pt] {CALLS} (method);
        \draw[lattice_gray_arrow, bend left=35] (file.west) to
            node[el, left=2pt] {IMPORTS} (file.east);
        \draw[lattice_gray_arrow, bend left=25] (class.east) to
            node[el, right=2pt] {INHERITS} (class.west);
    \end{tikzpicture}
    \caption[Graph database schema]{Neo4j schema used in CodeGraph. Files and classes are containers; functions and methods are leaves. Structural edges (\textsc{contains}, \textsc{imports}, \textsc{inherits}) and behavioral \textsc{calls} edges are stored separately.}
    \label{fig:database-schema}
\end{figure}

Database constraints enforce the identity rules from \cref{ch:design}. A uniqueness constraint on \texttt{qualified\_name} prevents duplicate entities during repository updates. Indexes on \texttt{file\_path} and \texttt{name} accelerate the lookups used during seed selection.

\subsection{Two-Pass Construction: Handling Forward References}

We load the node types introduced in \cref{sec:the-database} (\cref{fig:database-schema}) in two sequential passes to resolve forward references. We face a sequencing challenge when loading source code into a graph: a function in \texttt{main.py} may call a utility defined in \texttt{utils.py}, but if we process \texttt{main.py} first, the target node does not yet exist and the relationship creation fails.

\noindent To resolve this, we implement a two-pass loading mechanism:
\begin{enumerate}
    \item \textbf{Pass 1 (Entity Creation):} The system traverses the entire repository to create all structural nodes (\texttt{File}, \texttt{Class}, \texttt{Function}, \texttt{Method}) and their internal \texttt{CONTAINS} edges. By the end of this pass, every possible call target exists as a unique node in the database.
    \item \textbf{Pass 2 (Linkage):} With the node population complete, the system executes a second pass to process the staged \texttt{IMPORTS} and \texttt{CALLS} relationships, safely linking nodes that were created in Pass 1.
\end{enumerate}

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        node distance=0.55cm and 1.4cm,
        fn/.style={cgnodefunc, minimum width=1.6cm, font=\scriptsize\ttfamily},
        fl/.style={cgnodefile, minimum width=1.6cm, font=\scriptsize\ttfamily},
    ]
        \node[cgpanel, minimum width=5.8cm, minimum height=3.6cm] (p1) at (0,0) {};
        \node[anchor=north, font=\small\bfseries, yshift=-6pt] at (p1.north) {Pass 1: create nodes};
        \node[fl] at ([yshift=-0.15cm]p1.center) [xshift=-1.2cm] (f1a) {main.py};
        \node[fn, below=of f1a] (fn1a) {parse()};
        \node[fl, right=2.4cm of f1a] (f2a) {utils.py};
        \node[fn, below=of f2a] (fn2a) {tokenize()};
        \draw[lattice_gray_arrow] (f1a) -- (fn1a);
        \draw[lattice_gray_arrow] (f2a) -- (fn2a);

        \node[cgpanel, minimum width=5.8cm, minimum height=3.6cm, right=3.6cm of p1] (p2) {};
        \node[anchor=north, font=\small\bfseries, yshift=-6pt] at (p2.north) {Pass 2: link edges};
        \node[fl] at ([yshift=-0.15cm]p2.center) [xshift=-1.2cm] (f1b) {main.py};
        \node[fn, below=of f1b] (fn1b) {parse()};
        \node[fl, right=2.4cm of f1b] (f2b) {utils.py};
        \node[fn, below=of f2b] (fn2b) {tokenize()};
        \draw[lattice_gray_arrow] (f1b) -- (fn1b);
        \draw[lattice_gray_arrow] (f2b) -- (fn2b);
        \draw[lattice_arrow, bend left=28]
            (fn1b.north east) to node[font=\scriptsize\bfseries, fill=white, inner sep=1pt, above=1pt]
            {CALLS} (fn2b.north west);

        \draw[cgarr, line width=1.3pt] (p1.east) -- node[font=\footnotesize\bfseries, fill=white, inner sep=2pt, above=2pt] {2} (p2.west);
    \end{tikzpicture}
    \caption[Two-pass graph construction sequence]{Two-pass Neo4j loading. Pass~1 creates all file and function nodes with containment edges only. Pass~2 adds cross-file \textsc{calls} and \textsc{imports} once every target node exists.}
    \label{fig:two-pass-sequence}
\end{figure}

We execute all database writes using parameterized Cypher queries with batched \texttt{UNWIND} and \texttt{MERGE} clauses. This approach ensures idempotency; if we re-process a file, the system updates existing properties rather than creating duplicate nodes. Listing~\ref{lst:cypher-merge} demonstrates the MERGE pattern for both node and relationship creation.

\begin{cgcypher}{Neo4j write pattern}
// Pass 1: nodes (idempotent)
UNWIND $nodes AS node
MERGE (n:Function {qualified_name: node.qn})
  ON CREATE SET n.file_path = node.path
  ON MATCH  SET n.file_path = node.path

// Pass 2: relationships
UNWIND $edges AS rel
MATCH (a {qualified_name: rel.source}),
      (b {qualified_name: rel.target})
MERGE (a)-[:CALLS]->(b)
\end{cgcypher}
\captionof{lstlisting}{Cypher pattern for idempotent two-pass construction using MERGE.}
\label{lst:cypher-merge}

\subsection{GDS Projection}

Standard Cypher queries operate on the transactional database. However, executing graph algorithms like PageRank over millions of edges requires an in-memory representation. We use the Neo4j Graph Data Science (GDS) library to create an optimized undirected projection of the underlying graph. This projection allows the ranking algorithm to navigate bidirectionally, identifying both dependencies and their usage. We recreate this projection per query to ensure it reflects the latest state of the repository.

% =============================================================================
\section{The Engine: Ranking with PageRank}
\label{sec:the-engine}
% =============================================================================

When an AI agent requests context, the implementation enters \texttt{run\_core\_retrieval}. This function is the shared boundary between the user interfaces and the ranking logic. It prepares the BM25 index, extracts seeds, applies IDF weights, projects the graph into GDS, runs PPR, and formats source snippets under the token budget.

\subsection{Seed Selection and Personalization Vector}

The ranking process begins by identifying graph entry points, known as seeds, from the natural language task description. The implementation constructs a personalization vector by combining three documented signals:
\begin{itemize}
    \item \textbf{Entity Match:} Regex extraction identifies code identifiers, such as \texttt{AuthError}, in the prompt, matching them directly to graph nodes.
    \item \textbf{BM25 Search:} The engine executes a keyword search against the indexed entity names and docstrings to capture partial matches.
    \item \textbf{Issue Path Hint:} File paths or directory fragments mentioned in the task add weak location evidence.
\end{itemize}

These signals are summed and normalized into a probability distribution. The implementation stores both the seed weight and its source, allowing later explanations to report whether a result came from an entity match, a path hint, or BM25. The MCP surface still accepts \texttt{current\_file} for compatibility, but the retrieval pipeline ignores it because the active editor location was not a reliable relevance signal.

\subsection{Query-Time IDF Reweighting}

Before executing the traversal, we must address the hub node problem: generic utility functions called from hundreds of locations inevitably attract disproportionate relevance mass, obscuring domain-specific logic. 

To mitigate this, we implement a query-time Inverse Document Frequency (IDF) reweighting step. Prior to creating the GDS projection, a Cypher update calculates the in-degree for every target node. The weight of an incoming edge $(i, j)$ is adjusted using an inverse-logarithmic formula:
\begin{equation}
    w_{ij} = \frac{1}{\log_2(\text{in-degree}_j + 2)}
\end{equation}
Consequently, a highly connected utility function, such as \texttt{isinstance()}, receives a lower edge weight than a specialized domain method. This keeps relevance mass closer to code that carries task-specific information.

\subsection{PPR Execution}

With the personalization vector constructed and the weighted GDS graph projected, the engine invokes Personalized PageRank through Neo4j GDS. The production configuration uses uniform restart, damping factor $0.70$, and \texttt{top\_k = 30}. The final output is a list of code entities ranked by their relevance score, which post-processing turns into source snippets for the caller.

% =============================================================================
\section{The Interface: Serving AI Agents}
\label{sec:the-interface}
% =============================================================================

The internal ranking engine identifies structural dependencies with high precision, but this capability is inaccessible to autonomous agents without a standardized communication bridge. CodeGraph resolves this by implementing a Model Context Protocol (MCP) server. This section details the implementation of the server, specifically focusing on the use of the FastMCP framework, the mechanics of stateful resource management, and the automated context construction pipeline.

\subsection{Server Implementation and Tool Registration}

The server is implemented using the FastMCP Python framework, which provides a high-level abstraction over the JSON-RPC 2.0 communication layer. By default, the server communicates with the host application via Standard Input/Output (stdio). This transport layer ensures that the server can run as a lightweight subprocess, minimizing the resource footprint on the developer's machine.

The implementation exposes exactly two tools, matching the MCP contract described in \cref{sec:tool-interface}. The primary tool runs the retrieval pipeline and returns ranked source snippets. The second tool performs targeted structural lookup for known entities, including dependency traversal, symbol search, and class hierarchy queries. FastMCP registers both tools through Python decorators and derives the JSON-RPC schemas from their type annotations.

\subsection{Persistent State and Resource Management}

To sustain sub-250\,ms query latencies, the server must avoid the overhead of re-establishing database connections for every reasoning step. We achieve this by initializing core components, the Neo4j driver, the GDS client, and the structural ranker, at the server's module level.

Upon startup, the FastMCP server creates a persistent connection pool to the Neo4j instance. This persistent state is maintained throughout the server's lifecycle. When a tool is invoked, the ranker reuses the existing GDS projection and connection pool, as shown in the simplified implementation pattern in Listing~\ref{lst:mcp-server-impl}.

\begin{cgpython}{MCP server (simplified)}
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("CodeGraph")
ranker = Ranker(neo4j_url=CONFIG.url)

@mcp.tool()
async def get_relevant_context(task_description: str,
                               mentioned_entities: list[str] | None = None,
                               top_k: int = 0,
                               token_budget: int = 0):
    results = await ranker.find_relevant_code(
        task_description,
        mentioned_entities=mentioned_entities,
        top_k=top_k,
        token_budget=token_budget,
    )
    return assemble_context_package(results)

@mcp.tool()
async def query_dependencies(entity_name: str,
                             direction: str = "both",
                             depth: int = 1,
                             mode: str = "dependencies"):
    return dependency_service.query(entity_name, direction, depth, mode)
\end{cgpython}
\captionof{lstlisting}{FastMCP registration for the two agent-facing tools.}
\label{lst:mcp-server-impl}

\begin{cgshell}{Terminal}
$ codegraph query "Fix auth timeout in TokenExpiredError" \
    --entity TokenExpiredError --trace

Seeds:
  auth/exceptions.py::TokenExpiredError  entity_match
  auth/tokens.py                         issue_hint

Top results:
  1. auth/tokens.py::verify_token        0.042
  2. auth/middleware.py::authenticate    0.031
  3. auth/exceptions.py::TokenExpiredError 0.028
\end{cgshell}
\captionof{lstlisting}{Typical CLI retrieval output with seed trace.}
\label{lst:cli-query}

\begin{cgjson}{JSON response}
{
  "summary": {
    "result_count": 12,
    "token_budget": 6000,
    "visualizer_url": "http://localhost:8474"
  },
  "seeds": [
    {
      "qualified_name": "auth/exceptions.py::TokenExpiredError",
      "source": "entity_match",
      "weight": 0.15
    }
  ],
  "results": [
    {
      "entity_name": "verify_token",
      "entity_type": "Function",
      "file_path": "auth/tokens.py",
      "relevance_score": 0.042
    }
  ]
}
\end{cgjson}
\captionof{lstlisting}{Shape of a successful MCP retrieval response (source snippets omitted).}
\label{lst:mcp-response}

\subsection{Token-Aware Context Construction}

Once the Personalized PageRank algorithm identifies the highest-ranked entities, we transform these abstract graph nodes into a formatted text package. This construction process is governed by a strict, token-aware greedy algorithm.

The server uses a local Byte Pair Encoding (BPE) tokenizer to calculate the token count of every candidate code snippet in real-time. The construction loop iterates through the ranked results, reading raw source code from the filesystem based on the line boundaries stored in the graph. \noindent To ensure high information density, we apply two filters:
\begin{itemize}
    \item \textbf{File-Level Deduplication:} If multiple methods from the same file appear in the top results, the server includes the entire file once and skips subsequent hits for that file, avoiding redundant header inclusions.
    \item \textbf{Dynamic Budgeting:} The server maintains a running sum of tokens. If adding the next ranked entity would exceed the requested budget, defaulting to 6{,}000 tokens for MCP calls, the entity is skipped and the package is finalized.
\end{itemize}

\subsection{Error Handling and Feedback Loops}

The implementation returns explicit error payloads instead of silent empty context. If the graph is empty, the response includes a rebuild hint. If Neo4j or GDS fails, the response points the user to \texttt{codegraph doctor}. If no relevant result is found, the agent still receives structured metadata and can retry with more specific \texttt{mentioned\_entities}. The finalized payload, shown in \cref{lst:mcp-response}, gives the agent paths, scores, seed provenance, and source snippets in a machine-readable format.

% =============================================================================
\section{System Observability and the Visualizer Interface}
\label{sec:system-observability}
% =============================================================================

The interactive components of CodeGraph make the ranking process inspectable. The visualizer is not a separate retrieval algorithm; it is a debugging and demonstration interface over the same graph and retrieval pipeline used by the CLI and MCP tools.

\subsection{Topological Node Visualization}

The primary visualization pane renders the repository structure as a force-directed graph. Nodes represent parsed entities and edges represent relationships such as \textsc{contains}, \textsc{calls}, and \textsc{imports}. The same interface includes panels for seed provenance, node inspection, ranked results, project statistics, and source-code viewing. These views are useful when diagnosing why a query succeeded or failed, but they remain supporting tools rather than part of the retrieval algorithm itself.

% =============================================================================
\section{Summary}
\label{sec:summary}
% =============================================================================

This chapter described how CodeGraph turns the design from \cref{ch:design} into a working system. The implementation centers on Tree-sitter extraction, two-pass Neo4j construction, shared retrieval orchestration, and FastMCP delivery. These choices keep ranking logic in one core pipeline while exposing it through CLI, MCP, and visualizer interfaces. The next chapter evaluates how well this implementation retrieves target files on SWE-bench Lite.
