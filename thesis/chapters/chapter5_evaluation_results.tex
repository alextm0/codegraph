% ============================================================
%  Chapter 5: Evaluation and Results
% ============================================================
\chapter{Evaluation and Results}
\label{ch:evaluation}

We evaluate CodeGraph on SWE-bench Lite, a benchmark of 300 real GitHub issues paired with human-written fixes across 12 popular Python repositories. The task is file-level retrieval: given only a natural-language issue description and a repository, the system must rank source files by relevance and place the file modified in the human patch near the top of that list.

The optimal configuration reaches 75.7\% Recall@10, a 17.7 percentage point improvement over the competitive BM25-only baseline. This chapter details how the system reached that result. Rather than presenting the final numbers in isolation, we trace the engineering process: a diagnostic discovery about the bimodal structure of retrieval failures led to a Seed-First strategy that produced the dominant share of all performance gains.

% ============================================================
\section{Experimental Setup}
\label{sec:eval-framework}
% ============================================================

This section establishes the evaluation environment: the benchmark, metrics, protocol, and baselines used to measure retrieval quality.

\subsection{The SWE-bench Lite Benchmark}
\label{sec:eval-swebench}

SWE-bench Lite is a curated subset of 300 issues from SWE-bench, covering 12 popular Python projects such as Django, SymPy, and scikit-learn. Each instance provides the original issue text, a repository snapshot, and the human-written gold patch used for ground-truth labeling. \Cref{tab:repo-characteristics} summarizes the repository composition.

\begin{table}[H]
    \centering
    \small
    \caption{Repositories in SWE-bench Lite by instance count.}
    \label{tab:repo-characteristics}
    \cgtablerows
    \begin{adjustbox}{max width=\textwidth}
    \begin{tabularx}{\textwidth}{@{} >{\RaggedRight\bfseries}p{4.0cm} >{\RaggedRight\arraybackslash}p{1.8cm} >{\RaggedRight\arraybackslash}X @{}}
        \toprule
        \textbf{Repository} & \textbf{Instances} & \textbf{Domain} \\
        \midrule
        django/django & 114 & Web framework \\
        sympy/sympy & 77 & Symbolic mathematics \\
        matplotlib/matplotlib & 23 & Scientific visualization \\
        scikit-learn & 23 & Machine learning \\
        pytest-dev/pytest & 17 & Testing framework \\
        sphinx-doc/sphinx & 16 & Documentation generation \\
        Other (6 repositories) & 30 & Various (HTTP, arrays, etc.) \\
        \midrule
        \textbf{Total} & \textbf{300} & \\
        \bottomrule
    \end{tabularx}
    \end{adjustbox}
\end{table}

\subsection{Metrics and Protocol}
\label{sec:eval-metrics}

We evaluate retrieval at the file level, reporting three complementary metrics:
\begin{itemize}
    \item \textbf{Recall@$k$ (R@$k$):} The fraction of issues where the target file appears in the top-$k$ results. R@10 is our primary metric.
    \item \textbf{Mean Reciprocal Rank (MRR):} The average of $1 / \text{rank}$ for the target file, sensitive to rank quality.
    \item \textbf{Zero-recall count:} The number of issues where the target file does not appear in the top-10 results.
\end{itemize}

\subsection{Baselines}
\label{sec:eval-baselines}

We compare CodeGraph against three baselines:
\begin{itemize}
    \item \textbf{Random:} Establishes the performance floor.
    \item \textbf{BM25-only:} A sparse lexical retriever that establishes the lexical foundation without structural propagation. This achieves 58.0\% R@10.
    \item \textbf{One-hop ego graph:} An unranked structural baseline that returns all neighbors within one hop of the seeds. This achieves only 51.0\% R@10, demonstrating that multi-hop PPR traversal is necessary and superior to simple neighborhood expansion.
\end{itemize}

\subsection{Hardware and Environment}
All benchmarks run on an Apple M4 MacBook Pro with 16.0 GB RAM, macOS, Python 3.12.13, and Neo4j 2026.04.0.

% ============================================================
\section{Main Results and Performance Trajectory}
\label{sec:eval-trajectory}
% ============================================================

The BM25-only baseline achieves 58.0\% R@10, illustrating the limitations of pure lexical retrieval. Successive engineering stages refine graph propagation to reach 75.7\% R@10. \Cref{tab:trajectory-results} records the full trajectory.

\begin{table}[H]
    \centering
    \small
    \caption{Performance trajectory from the lexical baseline to the optimal structural configuration.}
    \label{tab:trajectory-results}
    \cgtablerows
    \begin{adjustbox}{max width=\textwidth}
    \begin{tabularx}{\textwidth}{@{} >{\RaggedRight\arraybackslash}X >{\RaggedRight\arraybackslash}p{1.2cm} >{\RaggedRight\arraybackslash}p{1.2cm} >{\RaggedRight\arraybackslash}p{1.2cm} >{\RaggedRight\arraybackslash}p{1.6cm} @{}}
        \toprule
        \textbf{Configuration} & \textbf{R@5} & \textbf{R@10} & \textbf{MRR} & \textbf{Zero-recall} \\
        \midrule
        BM25-only (Lexical Baseline)     & 46.7\% & 58.0\% & 0.354 & 126 \\
        Weighted PPR (Initial Graph)     & 60.7\% & 68.0\% & 0.440 & 96 \\
        Uniform PPR ($k=30$, Standard)   & 65.0\% & 74.0\% & 0.447 & 78 \\
        Uniform PPR ($k=50$, Final Optimal)    & \textbf{65.0\%} & \textbf{75.7\%} & \textbf{0.447} & \textbf{73} \\
        \bottomrule
    \end{tabularx}
    \end{adjustbox}
\end{table}

This trajectory serves as evidence for the Seed-First strategy. The gains are driven by three interventions: structural traversal (+10.0pp) from switching to a graph model; algorithm refinement (+6.0pp) from switching to uniform restart; and candidate budget optimization (+1.7pp) by increasing $k$ to 50.

\subsection{Seed Selection Improvements}
\label{sec:eval-seeds}

The Seed-First strategy acknowledges that improving seeds is more effective than refining the ranking algorithm. The implementation uses five interventions to implement this strategy:
\begin{enumerate}
    \item \textbf{Uniform PPR Restart:} Assigns equal restart mass to all seeds, letting graph topology resolve competition between them.
    \item \textbf{Compound Tokenization:} Decomposes CamelCase and snake\_case identifiers, allowing natural-language queries to match code identifiers.
    \item \textbf{Expanded BM25 Corpus:} Includes file paths, class names, and qualified names in the index.
    \item \textbf{Inverse-Frequency Entity Weighting:} Penalizes ambiguous seed names (e.g., \texttt{update}) proportional to their frequency in the graph.
    \item \textbf{Candidate Budget Optimization:} Increases $k$ to 30 to recover instances ranked just outside the initial threshold.
\end{enumerate}

% ============================================================
\section{Error Analysis}
\label{sec:eval-diagnostics}
% ============================================================

CodeGraph's retrieval failures are bimodal: the target file either ranks in the top-5 or does not appear in the top-10 at all. This section explains this pattern and classifies the remaining 78 failures into a structural taxonomy.

\paragraph{Bimodal Rank Distribution.}
For most issues, the target file either ranks in the top-5 or does not appear in the top-10. Only 15 out of 300 instances (5\%) fall in the intermediate band (ranks 6--10), as shown in \cref{fig:bimodal}. This reveals that PPR ranking is not the primary bottleneck: when the system identifies a useful starting point, it almost always ranks the target highly. The dominant failure mode is reachability: if no seed lies on a structurally connected path to the target, PPR has no signal to propagate there.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{assets/bimodal_rank_distribution.png}
    \caption{Bimodal rank distribution for the initial configuration. Instances land almost exclusively in Rank 1--5 or outside the top~10. This implies that reachability, not ranking, is the primary constraint.}
    \label{fig:bimodal}
\end{figure}

\paragraph{Codebase Structure and Retrieval Success.}
Performance varies by repository structure, as shown in \cref{tab:per-repo}. Subpackage-heavy repositories like scikit-learn (9\% failure) and matplotlib (13\%) yield high recall because their cohesive module clusters reinforce task-specific seeds. In contrast, repositories with large, heterogeneous directories like Django (27\%) or SymPy (31\%) create more difficulty: a single directory often mixes distinct concerns without explicit internal edges, limiting propagation. Plugin-driven architectures, such as Pylint (83\% failure), present an illustrative example of the limits of static analysis: many dependencies are registered dynamically and remain invisible to the graph.

\begin{table}[H]
    \centering
    \small
    \caption{Zero-recall failure rates by repository for the optimal configuration.}
    \label{tab:per-repo}
    \cgtablerows
    \begin{adjustbox}{max width=\textwidth}
    \begin{tabularx}{\textwidth}{@{} >{\RaggedRight\bfseries}p{4.2cm} >{\RaggedRight\arraybackslash}X >{\RaggedRight\arraybackslash}X >{\RaggedRight\arraybackslash}X @{}}
        \toprule
        \textbf{Repository} & \textbf{Instances} & \textbf{Zero-recall} & \textbf{Failure rate} \\
        \midrule
        scikit-learn      & 23 & 2  & 9\%  \\
        matplotlib        & 23 & 3  & 13\% \\
        django/django     & 114 & 31 & 27\% \\
        sympy/sympy       & 77 & 24 & 31\% \\
        Other (8 repositories) & 63 & 18 & 29\% \\
        \midrule
        \textbf{Total} & \textbf{300} & \textbf{78} & \textbf{26\%} \\
        \bottomrule
    \end{tabularx}
    \end{adjustbox}
\end{table}

\paragraph{Structural Failure Taxonomy.}
An analysis of the 78 zero-recall instances identifies two distinct failure modes (\cref{fig:taxonomy}):
\begin{itemize}
    \item \textbf{Connectivity Gaps (40 instances):} Occur when seeds land in the correct part of the codebase but no explicit edge connects them to the target file. This reflects the inherent limitation of static analysis for files that are logically related but structurally isolated.
    \item \textbf{Semantic Gaps (38 instances):} Arise from abstract natural-language descriptions with no lexical overlap with source code identifiers. Without a lexical match, the BM25 stage cannot place any seed in the relevant neighborhood.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{assets/failure_taxonomy.png}
    \caption{Failure taxonomy of the 78 zero-recall instances.}
    \label{fig:taxonomy}
\end{figure}

% ============================================================
\section{Ablations and Sensitivity Analysis}
\label{sec:eval-ablations}
% ============================================================

This section justifies our design decisions through controlled ablation studies, measuring the individual contribution of each component to the final performance.

\subsection{Algorithm Selection: Uniform vs. Weighted Restart}
\label{sec:eval-ablation-algorithm}

The most consequential choice is the restart distribution. Switching from weighted restart (68.0\%) to uniform restart (74.0\%) raises R@10 by 6.0pp. While weighting by confidence amplifies false positives from ambiguous seeds, uniform restart lets graph topology resolve where probability accumulates. This is consistent with the bimodal observation: the ranking function works correctly once given a useful starting point.

\subsection{Structural Ablations}
\label{sec:eval-ablation-structure}

We isolate the value of structural features by removing individual edge types and analyzing the impact on R@10 compared to the standard Uniform PPR baseline (74.0\%). \Cref{tab:ablations} summarizes the results.

\begin{table}[H]
    \centering
    \small
    \caption{Ablation results illustrating the contribution of individual graph components compared to the standard Uniform PPR configuration (R@10=74.0\%, Zero-recall=78).}
    \label{tab:ablations}
    \cgtablerows
    \begin{adjustbox}{max width=\textwidth}
    \begin{tabularx}{\textwidth}{@{} >{\RaggedRight\arraybackslash}X >{\RaggedRight\arraybackslash}p{1.6cm} >{\RaggedRight\arraybackslash}p{1.6cm} >{\RaggedRight\arraybackslash}p{2.0cm} @{}}
        \toprule
        \textbf{Ablation} & \textbf{R@10} & \textbf{$\Delta$ R@10} & \textbf{Zero-recall} \\
        \midrule
        \texttt{directed\_natural} (forward only) & 71.7\% & -2.3pp & 85 \\
        \texttt{directed\_reverse} (backward only) & 72.7\% & -1.3pp & 82 \\
        \texttt{no\_calls} & 73.0\% & -1.0pp & 81 \\
        \texttt{no\_imports} & 73.3\% & -0.7pp & 80 \\
        \texttt{no\_inherits} & 74.0\% & 0.0pp & 78 \\
        \texttt{no\_idf} (unweighted seeds) & 75.3\% & +1.3pp & 74 \\
        \bottomrule
    \end{tabularx}
    \end{adjustbox}
\end{table}

The data confirms that bidirectional traversal is strictly better than unidirectional traversal. Removing call graph edges causes the largest performance drop among edge types (-1.0pp). Interestingly, removing IDF weighting actually improved performance slightly (+1.3pp), suggesting that for uniform PPR, allowing frequent ambiguous terms to act as uniform seeds relies entirely on the graph to resolve relevance, which it does effectively.

\subsection{Hyperparameter Sensitivity: Budget and Damping}
\label{sec:eval-ablation-sensitivity}

We sweep over the candidate budget $k$ and the PPR damping factor $d$ to characterize sensitivity.
\begin{itemize}
    \item \textbf{Candidate Budget ($k$):} Increasing $k$ from 30 to 50 yields a 1.7pp gain.
    \item \textbf{Damping Factor ($d$):} The system is robust between $d=0.30$ and $d=0.70$. Lowering $d$ to 0.30 drops R@10 to 72.7\% and $d=0.50$ drops it to 73.0\%, confirming that the default $d=0.70$ provides the optimal balance. Higher values confine the random walk too close to the seeds, while lower values cause excessive diffusion.
\end{itemize}

Connectivity Gaps arise from missing explicit edges, and Semantic Gaps arise from lexical mismatch. Together, they account for all 78 zero-recall instances and establish the boundary conditions that \cref{ch:discussion} interprets and \cref{ch:conclusion} addresses through future work.
