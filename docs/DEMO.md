codegraph query "How is the Personalized PageRank damping factor configured?" --entity PPRConfig
codegraph explain "How is the PPRConfig damping factor configured?"
codegraph analyze deps run_core_retrieval

Repo : https://github.com/alextm0/codegraph

PROMPT MCP: Using the CodeGraph MCP, look up how the Personalized PageRank damping factor is configured and suggest how I might change the default to 0.85. 

Entity: PPRConfig
---
